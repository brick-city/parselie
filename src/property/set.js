/**
 *
 * @param {Object} ctx
 * @param {Map< String, Object >} ctx.properties
 * @param {*} key
 * @param {*} value
 */

export function setProperty(ctx, key, value) {

    if (obj[key] === undefined) {

        obj[key] = value;

    }

}
